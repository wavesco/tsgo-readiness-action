{
  "name": "tsgo-readiness-action",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "package": "npm run build && ncc build lib/index.js -o dist",
    "all": "npm run package"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.7.3",
    "@vercel/ncc": "^0.38.1"
  }
}
